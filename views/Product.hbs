<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Products</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h2 class="text-center mb-4">Product List</h2>
        <table class="table table-bordered table-dark table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Category</th>
                    <th>Price</th>
                    <th>Offer Price</th>
                    <th>Stock</th>
                    <th>Images</th>
                    <th>Colors</th>
                    <th>Specifications</th>
                    <th>Release Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each products}}
                <tr>
                    <td>{{this.name}}</td>
                    <td>{{this.category}}</td>
                    <td>${{this.price}}</td>
                    <td>{{#if this.offerPrice}} ${{this.offerPrice}} {{else}} N/A {{/if}}</td>
                    <td>{{this.stock}}</td>
                    <td>
                        {{#each this.productImages}}
                        <img src="{{this}}" alt="Product Image" style="width: 50px; height: auto;">
                        {{/each}}
                    </td>
                    <td>
                        {{#each ../this.colorOptions}}
                        <div
                            style="background-color: {{this.colorCode}}; width: 20px; height: 20px; display: inline-block;">
                        </div>
                        {{/each}}
                    </td>
                    <td>
                        <ul>
                            {{#each ../this.specifications}}
                            <li><strong>{{@key}}:</strong> {{this}}</li>
                            {{/each}}
                        </ul>
                    </td>
                    <td>{{this.releaseDate}}</td>
                    <td>
                        <button class="btn btn-primary btn-sm">Edit</button>
                        <button class="btn btn-danger btn-sm">Delete</button>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>

</html>